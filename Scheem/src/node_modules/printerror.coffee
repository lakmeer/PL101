module.exports = (err) ->
    if err.name is "AssertionError"
        console.error " >> #{err.name} :#{ if err.operator? then ': ' + err.operator else '' }"
        console.error "    expected", err.expected
        console.error "    received", err.actual
    else
        console.error err
